# Ephemeral per-run state (ledger, runlog). Folded into the PR, not committed.
# .steward/metrics.jsonl is deliberately NOT ignored — it is committed.
# Policy authority: docs/CONVENTIONS.md.
.steward/runs/
